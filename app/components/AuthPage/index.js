// @flow
import React from 'react';

const AuthPage = () => {
  return (
    <div>
      <h3>ログインページ</h3>
      /* authInfo: userID.length === 0 then * show LoginForm * else * show logoutForm */
    </div>
  );
};

export default AuthPage;
